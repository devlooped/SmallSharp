<Project>

  <PropertyGroup>
    <LangVersion>Preview</LangVersion>
  </PropertyGroup>

  <ItemGroup>
    <Content Include="*$(DefaultLanguageSourceExtension)" />
    <Compile Include="$(ActiveDebugProfile)" Condition="Exists('$(ActiveDebugProfile)')" />
  </ItemGroup>

</Project>